<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai.jobpilotbackend.resume.mapper.AitaskMapper">

    <resultMap id="BaseResultMap" type="com.ai.jobpilotbackend.resume.entity.AiTask">
        <id     column="id"         property="id"/>
        <result column="user_id"    property="userId"/>
        <result column="resume_id"  property="resumeId"/>
        <result column="job_desc"   property="jobDesc"/>
        <result column="status"     property="status"/>
        <result column="result"     property="result"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_task (user_id, resume_id, job_desc, status, created_at, updated_at)
        VALUES (#{userId}, #{resumeId}, #{jobDesc}, #{status}, NOW(), NOW())
    </insert>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM ai_task WHERE id = #{id}
    </select>

    <update id="updateResult">
        UPDATE ai_task
        SET result = #{result}, status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE ai_task
        SET status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
